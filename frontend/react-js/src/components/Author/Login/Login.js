import React from 'react';
import './style.css'

const Login = ({changePage}) => {
    return (
        <div className="login">
            <h1>Login</h1>
            <button>Registrati</button>
        </div>
    )
}

export default Login;